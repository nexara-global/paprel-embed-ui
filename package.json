{
  "name": "paprel-embed-ui",
  "private": true,
  "description": "Paprel embedded accounting SDK monorepo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @paprel/embed-core && npm run build -w @paprel/ui && npm run build -w @paprel/accounting && npm run build -w @paprel/reports",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run build && npm run lint --workspaces --if-present",
    "pack:check": "node scripts/pack-check.mjs",
    "consumer:check": "node scripts/consumer-check.mjs",
    "version:packages": "changeset version",
    "release": "npm run build && npm test && npm run pack:check && changeset publish"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.1",
    "typescript": "^7.0.2"
  }
}
