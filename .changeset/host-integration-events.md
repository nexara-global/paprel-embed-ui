---
"@paprel/embed-core": minor
"@paprel/accounting": minor
---

Add versioned host integration events for resource navigation and collection view state. Accounting lists now emit namespaced events for resource opens, search, filters, tabs, and pagination, while retaining their legacy component-specific events when the new resource event is not canceled.
